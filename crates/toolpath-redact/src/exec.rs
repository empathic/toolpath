//! An out-of-process detector: one JSON candidate per line on stdin, one
//! array of findings on stdout.
//!
//! Not implemented. The subprocess detector was cut from the first
//! release; the module records the wire contract so the next attempt does
//! not have to re-derive it.
